# Knowledge Base

Place markdown (`.md`) or text (`.txt`) files here. Their content is loaded and injected into the system prompt so the assistant can answer using this knowledge.

Files are loaded in alphabetical order and concatenated with a separator.
